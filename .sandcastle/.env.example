# Intentionally empty. The checked-in runners use CODEX_ACCESS_TOKEN when the
# caller provides it; otherwise they mount only the host Codex auth file so the
# official CLI can refresh the login. Never store Codex or GitHub credentials
# here. Run `codex login` on the host instead of creating .sandcastle/.env.
