# No-change smoke test

Read `AGENTS.md`, `docs/PRODUCT_HANDOFF.md`, `README.md`, and `SECURITY.md`.
Run `dotnet --version`, then briefly report the product purpose, the required
.NET SDK version, and one release limitation documented in those files.

Do not modify, create, delete, rename, stage, or commit any file. Do not change
Git configuration, branches, refs, dependencies, or remotes. Do not use GitHub,
`gh`, push, deploy, publish, or create a release. Do not print environment
variables or credentials.

Finish with exactly `<promise>COMPLETE</promise>`.
